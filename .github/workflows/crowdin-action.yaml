name: Crowdin Sync

on:
  push:
    branches: [ "dev" ]
    paths:
      - "nfcworkflow/app/src/main/res/values/strings.xml"
      - "crowdin.yml"
      - ".github/workflows/crowdin-action.yaml"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: crowdin-dev
  cancel-in-progress: true

jobs:
  crowdin-sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Upload sources
        uses: crowdin/github-action@v2
        with:
          config: crowdin.yml
          upload_sources: true
          upload_translations: false
          crowdin_branch_name: dev
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}

      - name: Download translations
        uses: crowdin/github-action@v2
        with:
          config: crowdin.yml
          download_translations: true
          skip_untranslated_strings: true
          crowdin_branch_name: dev
          localization_branch_name: l10n_dev
          push_translations: true
          push_sources: false
          create_pull_request: true
          pull_request_base_branch_name: dev
          pull_request_title: ":globe_with_meridians: sync Crowdin translations"
          pull_request_body: "Automated translation sync from Crowdin"
          pull_request_labels: "localization,l10n"
          pull_request_assignees: "Guest-Liang"
          pull_request_reviewers: "Guest-Liang"
          commit_message: ":globe_with_meridians: sync translations from Crowdin"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}